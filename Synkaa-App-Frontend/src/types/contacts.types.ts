export interface UpdateContactTagType {
  success: boolean;
  message: string;
}

export interface DeleteContactTagType {
  success: boolean;
  message: string;
}

export interface SearchContactType {
  success: boolean;
  message: string;
  data: any;
}
